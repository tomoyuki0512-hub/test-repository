# CLAUDE.md

## 作業ルール

### ブランチ
- **ブランチは作成しない**
- すべての変更は `main` ブランチで直接行う

### コミット・プッシュ
- ファイルを作成・更新したら**その都度すぐにコミットしてプッシュする**
- まとめてプッシュせず、変更のたびに `git push origin main` を実行する

### プッシュコマンド
```bash
git add <file>
git commit -m "メッセージ"
git push origin main
```
